/**
 * Questioning layer (Layer 8,  stage 6).
 *
 * `selectNextQuestion` picks the max-information-gain, safety-filtered playbook
 * step; `informationGain` is the exported discriminating-power heuristic.
 */
export * from './select-next-question.js';

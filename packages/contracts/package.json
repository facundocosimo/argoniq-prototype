{
  "name": "@argoniq/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared API request, response, pagination, and source-reference types.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@argoniq/core-domain": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@argoniq/config": "workspace:*",
    "eslint": "^9.39.5",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}

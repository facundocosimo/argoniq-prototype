{
  "name": "@argoniq/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Logging, application errors, request context, and audit events.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@argoniq/contracts": "workspace:*",
    "@argoniq/core-domain": "workspace:*",
    "pino": "^10.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@argoniq/config": "workspace:*",
    "@types/node": "^26.4.1",
    "eslint": "^9.39.5",
    "pino-pretty": "^13.1.3",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}
